@page "/org/associates"
@inherits BaseAdminComponent<AssociatesViewModel>
@layout MainLayout

<PageTitle>@ViewModel.Title</PageTitle>

<div class="row mt-2">
    <div class="col">
        <h1>@ViewModel.Title</h1>
    </div>
</div>
<div class="row">
    <div class="col">
        <table class="table table-sm mt-2">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Name</th>
                    <th scope="col">Permissions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var assoc in ViewModel.PageList) {
                    <tr>
                        <td>@assoc.Id</td>
                        <td>@assoc.PublicName</td>
                        <td>@assoc.PermissionSet.Name</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<div class="row">
    <div class="col">
        <ListViewPageBar ViewModel="@ViewModel" />
    </div>
</div>

